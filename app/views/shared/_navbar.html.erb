<div class="navbar navbar-expand navbar-light navbar-lewagon">
  <%= link_to "/", class: "navbar-brand" do %> <%= image_tag "logo.png" %> <%
  end %>
    <!-- <%= link_to new_event_path, class: "navbar-plus" do %>
  <%= image_tag "plus.svg" %>
  <% end %> -->
    <div class="nav-btn">
      <% if user_signed_in? %>
        <div class="friends-notification">
          <% if @friend_requests.count > 0  %>
            <%= link_to pages_friends_path, title: "Invitations Amis" do %> 
              <%= @friend_requests.count %>
            <% end %>
          <% end %>
        </div>
      <% end %>
      <div class="star btn-nav">
        <%= link_to favorites_path, class: "navbar-clip" do %> <%= image_tag "heart.svg" %>
          Favoris <% end %>
      </div>
      <div class="friends-icon btn-nav">
        <%= link_to pages_friends_path, class: "navbar-clip" do %> <%= image_tag "friends.svg" %>
          Amis <% end %>
      </div>
      <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <% if user_signed_in? %>
            <!-- <li class="nav-item active">
          <%= link_to "Home", "#", class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Messages", "#", class: "nav-link" %>
        </li> -->
            <li class="nav-item dropdown">
              <%= image_tag "scream.svg", class: "avatar-large dropdown-toggle", id:
          "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true,
          'aria-expanded': false %>
              <div
            class="dropdown-menu dropdown-menu-right"
            aria-labelledby="navbarDropdown"
          >
                <%= link_to "Mon compte", edit_user_registration_path, class:
            "dropdown-item" %> <%= link_to "Ma liste de films", favorites_path,
            class: "dropdown-item" %> <%= link_to "Organiser un ciné",
            new_event_path, class: "dropdown-item" %> <%= link_to "Déconnexion",
            destroy_user_session_path, method: :get, class: "dropdown-item"
            %>
              </div>
              <i class="fas fa-caret-down" id="triangle"></i>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Connexion", new_user_session_path, class: "nav-link" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
